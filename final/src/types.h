#ifndef TYPES_H
#define TYPES_H

typedef enum{false, true} bool;
typedef enum{port_flg, logfile_flg, poolSize_flg, dataset_flg} flg;
typedef enum{id_flg, address_flg, port_flg_cli, path_flg} flg_client;

#endif